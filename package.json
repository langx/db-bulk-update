{
  "name": "db-bulk-update",
  "version": "0.0.1",
  "description": "Update user collection in bulk",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-appwrite": "^12.0.1"
  }
}
